<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/<%= site.tailwind %>">
    <% (page[:css] || site.css).each do |css| -%>
      <link rel="stylesheet" href="/<%= css %>">
    <% end -%>
    <link rel="icon" href="/assets/favicon.png">
    <link rel="alternate" type="application/rss+xml" title="<%= site.title %>" href="/feed.xml">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Literata:ital,opsz,wght@0,7..72,200..900;1,7..72,200..900&display=swap" rel="stylesheet">
    <title><%= page[:title] ? "#{page[:title]} -" : "" %> <%= site.title %></title>
  </head>
  <body>
    <header>
      <nav class="print:hidden">
        <ul>
          <% site.navigation.each do |item| -%>
            <li class="<%= 'current' if page[:file] == item[:file] %>">
              <a href="/<%= item[:file] %>"><%= item[:name] %></a>
            </li>
          <% end -%>
        </ul>
      </nav>
    </header>
    <main class="page-content" aria-label="Content">
      <div class="<%= page[:wrapper] || "prose" %>">
        <%= content -%>
      </div>
    </main>
    <footer class="print:hidden">
      <p>&copy; 2004 &mdash; <%= Time.now.year %> Mike Dalessio &middot; <a class="text-inherit no-underline" href="/feed.xml">RSS <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="inline-block w-4 h-4 -translate-y-0.5"><circle cx="6.18" cy="17.82" r="2.18"/><path d="M4 4.44v2.83c7.03 0 12.73 5.7 12.73 12.73h2.83c0-8.59-6.97-15.56-15.56-15.56zm0 5.66v2.83c3.9 0 7.07 3.17 7.07 7.07h2.83c0-5.47-4.43-9.9-9.9-9.9z"/></svg></a></p>
    </footer>
  </body>
</html>
